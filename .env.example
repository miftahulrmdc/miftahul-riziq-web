# Copy to .env on the server and fill in. .env is gitignored — the token alone
# is enough for someone to run your tunnel, so it never belongs in the repo.
#
# Cloudflare Zero Trust dashboard → Networks → Tunnels → your tunnel →
# Configure → copy the token from the Docker install command.
TUNNEL_TOKEN=
